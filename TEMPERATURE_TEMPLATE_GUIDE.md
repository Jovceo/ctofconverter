# 智能温度转换 SEO 模板开发与使用指南
## (Intelligent Temperature Conversion Template Guide)

本文档通过详细的技术说明和操作指南，帮助开发者理解和维护 `pages/temperature-template.tsx` 及其配套生态系统。这是一个专为 **Programmatic SEO** 设计的高级页面模板，旨在批量生成数以万计、内容独特且高度符合用户搜索意图的着陆页。

---

## 1. 产品概述

### 核心目标
解决传统批量生成页面（Programmatic SEO）面临的 **"内容同质化"** 和 **"缺乏用户价值"** 两大痛点。

### 解决方案
采用 **"固定地基 + 动态拼图"** 的架构：
*   **地基**：提供最准确的转换计算、公式推导、可视化图表。
*   **拼图**：根据用户的**搜索关键词**（如 "fever", "oven"）和**温度数值**（如 38, 180），智能注入特定的警告、建议和问答。

---

## 2. 核心特性 (Key Features)

### 2.1 🦎 变色龙策略 (Content Strategy)
页面不再是静态的，而是根据上下文自动切换模式：
*   **Health Mode**: 检测到 35-42°C 或 "fever" 词汇 -> 显示医疗风格，隐藏烤箱表，弹出高烧警告。
*   **Cooking Mode**: 检测到 >60°C 或 "oven" 词汇 -> 显示烤箱刻度表，提供食品安全建议。
*   **Weather Mode**: 检测到 -50~50°C -> 显示穿衣指数和体感描述。

### 2.2 🧠 内置知识引擎 (Knowledge Engine)
位于 `utils/contentStrategy.ts`，内置了一个微型知识库。
*   **自动化**：当检测到 `tea`（茶）、`chicken`（鸡肉）、`scald`（烫伤）等关键词时，自动从知识库提取专业的 Insights（洞察卡片）和 FAQs。
*   **扩展性**：不仅是简单的转换，而是回答 "Is 75°C safe?" 这种深层问题。

### 2.3 🌀 文案防重机制 (Content Spinning)
位于 `utils/textSpinner.ts`，解决了 Google 判定重复内容的风险。
*   **原理**：利用“确定性随机”（Deterministic Randomness），根据温度数值生成不同的句式变体。
*   **效果**：0°C 的页面说 "First, multiply by..."，而 1°C 的页面说 "Begin calculation by..."。这对 SEO 极其重要。

### 2.4 🖼️ 动态社交图片 (Edge OG Image)
位于 `pages/api/og.tsx`。
*   **功能**：实时为每个温度生成一张精美的分享卡片（Open Graph Image）。
*   **性能**：运行在 Edge Runtime 上，速度极快。
*   **SEO**：让 Google 图片搜索和社交媒体分享更具吸引力。

---

## 3. 系统架构与文件结构

该模块由以下核心文件组成：

```
/
├── pages/
│   ├── temperature-template.tsx  # [核心页面] 负责UI渲染、组件拼装
│   └── api/
│       └── og.tsx                # [动态图片] Serverless 绘图服务
├── utils/
│   ├── contentStrategy.ts        # [大脑] 决定页面显示什么内容、知识库所在
│   ├── textSpinner.ts            # [文案] 负责生成不重复的句式
│   └── temperaturePageHelpers.ts # [工具] 基础计算、Schema生成
└── public/
    └── images/equation/          # (虚拟路径) 指向 api/og 的Rewrite规则
```

---

## 4. 开发与配置指南

### 4.1 如何添加新的“知识点”？
如果你想让页面针对 "Coffee"（咖啡）无论什么温度都显示特定建议：

1. 打开 `utils/contentStrategy.ts`。
2. 找到 `const KNOWLEDGE_BASE` 对象。
3. 添加新的 Key-Value：
   ```typescript
   'coffee': {
     insights: [{
       type: 'tip',
       title: 'Barista Tip',
       content: 'Coffee is best brewed between 90°C and 96°C.'
     }],
     faqs: [{ question: '...', answer: '...' }]
   }
   ```
4. 保存即可。下次调用 `generateContentStrategy(92, 'coffee')` 时会自动生效。

### 4.2 如何修改文案变体库？
如果你觉得计算步骤的英文表达太单调：

1. 打开 `utils/textSpinner.ts`。
2. 在 `STEP1_VARIANTS` 或 `CONCLUSION_VARIANTS` 数组中添加新的字符串模板函数。
   ```typescript
   (c: number) => `Simply take ${c} and multiply it by 1.8 to start.`
   ```
3. 系统会自动将其纳入随机池。

### 4.3 生产环境部署 (Production Deployment)
目前的 `temperature-template.tsx` 是一个**演示/开发容器**。在正式上线时，你需要：

1. **创建动态路由**：建立 `pages/[celsius].tsx` 或使用 `getStaticPaths`。
2. **数据注入**：
   在 `getStaticProps` 中：
   ```typescript
   export const getStaticProps = async ({ params }) => {
     const celsius = parseFloat(params.celsius);
     // 1. 获取基础策略
     const strategy = generateContentStrategy(celsius, 'user-search-keyword'); 
     
     return { props: { celsius, strategy } };
   }
   ```
   *注意：关键词 (`user-search-keyword`) 需要你从 SEO 数据库或 URL 参数中获取。*

### 4.4 性能优化建议
*   **图片缓存**：确保 CDN 对 `/api/og` 的响应设置了 `Cache-Control: public, max-age=31536000`。
*   **内链优化**：在 `RelatedTemperatures` 组件中，尽量链接到“语义相关”的页面（如从 38°C 链到 39°C 发烧页面），而不仅仅是数字相邻。

---

## 5. 测试与验证

### 本地演示模式
1. 打开 `pages/temperature-template.tsx`。
2. 滚动到底部 `TemperatureTemplatePage` 组件。
3. 修改 `const testCelsius = 75;`。
4. 运行 `npm run dev` 并访问 `/temperature-template`。
5. 观察：页面应该自动显示 "Water Safety" 警告。

### 检查清单
- [ ] **UI检查**：是否根据温度显示了正确的组件（如烤箱表 vs 发烧警告）？
- [ ] **SEO检查**：View Source 查看 `<title>` 和 `<meta description>` 是否包含关键词。
- [ ] **Schema检查**：使用 Google Rich Results Test 验证 FAQ 和 How-to 结构化数据。
- [ ] **图片检查**：确认 Og Image 链接是否可访问且生成正确。

---

**版本**: 2.0 (Intelligent Engine Update)
**最近更新**: 2025-12-16
