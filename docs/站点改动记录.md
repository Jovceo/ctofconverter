# 站点改动记录

> 记录每次实际代码改动，按日期倒序排列

---

## 2026-08-27（下午）：数据资产 v1 + 站内医学数值纠错 + Gas Mark 纠错

**背景**：上午定了方向——本域名不再是收入载体，转而做「陌生人可以直接拿走用」的公开数据资产（数据集 + 免注册 API + 可嵌入组件），一周时间盒验证「有没有人用」。本轮是该项的 v1 全部代码，尚未部署（上一轮 `noindex` 变更也未部署：`git status` 显示当时并没有 push 成功，线上 `/es` 仍是 `index, follow`，已实测确认）。

### 新增：数据层

- `config/datasets/oven-conversions.json`（12 行×7 列）、`safe-internal-temperatures.json`（9×5）、`human-body-temperature.json`（34×20）——唯一数据源，每行带 `source`/`source_url`/`quote`/`checked_on`
- `utils/refDatasets.ts`：注册表 + CSV/投影/过滤工具 + `WITHHELD_DATASETS`（未核实的数据集让 API 返回 503 + 理由，不静默 404）
- `scripts/publish-dataset.js`（`prebuild` 自动跑）：生成 `public/data/{id}.json|csv` + `manifest.json` + `index.html` 目录页
- `pages/api/ref/index.ts`、`[dataset].ts`（`?format=csv` / `?select=` / `?q=`）、`lookup.ts`（换算 + 归档，不插值；`context=body` 默认只应用 `setting=home` 的行，支持 `?person=` 年龄过滤、对 30–45 °C 之外的值给 `plausible:false`）
- `public/embed/ref-chart.js`：零依赖嵌入表格（`data-set/data-cols/data-title/data-max/data-mount/data-theme`），自带返回署名链接
- `public/embed/demo.html`（`noindex, follow`）：三张表实盘演示；`pages/data-api.tsx`：文档页（清单从 manifest 自动渲染）
- `components/Footer.tsx` 新增英文入口链；`package.json` prebuild 接入发布脚本

### 修正：站内错误数据

- `locales/en/fan-oven-conversion-chart.json`：原表格把常规烤箱值标成风扇值，GM 3/5/7 全错，GM 9 的 °F 写成 465（应 475），并有一句「Gas mark 4 is equivalent to 180 °C in a fan oven」（应为常规 180 °C / 风扇 160 °C）。按 Wikipedia/BBC Good Food/Which? 重做成 12 行表 + 风扇−20 °C 惯例单独说明；`pages/fan-oven-conversion-chart.tsx` 同步。
- `locales/en/body-temperature-chart-fever-guide.json` + 同名页面：`<36.0 °C = Hypothermia` 错（医学定义 <35.0 °C；健康成人腋温均值就是 35.97 °C）→ 拆成 `hypothermia <35.0 °C（急症）` 与 `lowTemp 35.0–36.4 °C（偏低，复测）`；交互评估里成人/儿童的 35.0–35.9 不再显示「Hypothermia/Danger」（infant 分支保留原有更严的 <36.0 判定）；腋温备注「0.5–1.0 °C lower」属单位串位（权威为 0.3–0.6 °C = 0.5–1.0 °F），改为偏移口径；直肠 0.5 °C→0.6 °C（Merck）；测量表里无出处的「各部位正常范围」换成可引的偏移值。
- 核查台账与逐源原句：[docs/数据来源核查-2026-08-27.md](./数据来源核查-2026-08-27.md)。

### 验证

- `npx tsc --noEmit` exit=0；`npm run lint` 0 error / 24 warning（与基线同数）；`npm run build` 成功（650 页，API 路由为 ƒ、`/data-api` 为 ●）
- 本地 `next start :3112` 实测：`/api/ref` 三数据集（withheld=0，无中文泄漏）、`/api/ref/human-body-temperature` 34 行×20 列、CSV 36 行且表头带单位、`?q=hypothermia` 9 行、`select` 裁剪生效；`lookup` 180 °C→GM 4 exact、175 °C→最近 GM 3（距离 5）、37.8 °C→仅 StatPearls low-grade、35.4 °C→below-normal+neonate、`setting=all` 才出住院/参考行、`person=adult|infant` 互斥正确；未知数据集 404、非法 value 400；`Access-Control-Allow-Origin: *`
- 页面：`/data-api` = 200 / `index, follow` / 无 hreflang；`/es/data-api` = `noindex, follow`；`/` = `index, follow`，`/es` = `noindex, follow`（上一轮止血逻辑本地确认生效）
- **嵌入组件真实浏览器测出的致命 bug**：`document.currentScript` 在 `DOMContentLoaded` 回调里是 `null`，导致三个表全部静默不渲染（只有 `node --check` 时会看起来没问题）。已改为脚本执行时就先把 `currentScript` 存住；同时 `ORIGIN` 改为从脚本自身 URL 推导（本地/staging 可测）。复测：三张表均渲染、标题/表头带单位/署名行正确、console 0 error。

### 待办

1. 站长 `git push`（本轮与上午的 `noindex` 都在同一个未推提交里）→ Vercel 自动部署 → 用部署后 curl 清单验收
2. 上线日才开始计「一周有没有陌生人用」；未上线前不计时间
3. 是否把 `public/data/` + 组件另开一个独立 GitHub 仓库（对外发布，需站长点头）

---

## 2026-08-27：收录实测诊断 + 语言矩阵止血

**动作**：用 GSC API（Search Analytics + URL Inspection）与 Bing Webmaster API（GetCrawlStats / GetUrlInfo / Quota）实测两侧收录状态，结论写入 [docs/收录诊断-2026-08-27.md](./收录诊断-2026-08-27.md)。复现脚本与原始 JSON 存在 `E:\工具\SEO数据拉取工具\analysis\gsc_index_check_20260827\`。

**关键事实**：Google sitemap `submitted 62 / indexed 0`，sitemap 最后被取 2026-07-28；40 个 URL 抽样仅首页 "Submitted and indexed"，33 个 "Crawled – currently not indexed"（其中 5 个 8 月刚被抓过且抓取成功）；近 28 天仅 8 页有展示（579 imp / 0 clk），其中 7 页是语言矩阵孤儿页；月度展示 2026-04 15,899 → 05 66（断崖在 4-26/27）。Bing 侧 `InIndex` 8-18 见底 172 → 8-25 回升 185，昨天仍在抓关键页，提交配额 1 万/天基本闲置。

**待批**→**已做（同日中午，站长将决定权交给 agent）**：P0 语言矩阵全站 `noindex, follow`（与 v2.2 冻结令冲突，经站长授权后执行）；hreflang 模板级收敛。P1 sitemap 瘦身、第二批页精做、逐页 desc 重写——**作废**，理由见下。

### 代码变更（本轮，未部署）

- `utils/locale-config.ts`：新增 `getRobotsDirective(locale)` / `isIndexableLocale(locale)`——只有 `en` 可索引；非英语 locale 强制 `noindex, follow`。函数注释内写定背景与禁令。
- `components/Layout.tsx`：`robots` 改为走 `getRobotsDirective`（非英语页不接受 `seo.robots` 覆盖）；Head 内不再输出 10 语言 `rel="alternate"`，英语页只声明自身 `en` + `x-default`，非英语页 0 条。语言切换器 UI 继续用 `visibleAlternateLinks` context，未受影响。
- `pages/index.tsx`、`pages/fahrenheit-to-celsius.tsx`：这两页自带 `<Head>`（会覆盖 Layout），同步接 `getRobotsDirective` + 非英语页不输出 alternate；fahrenheit-to-celsius 删除 `getAlternateUrls` 多语言声明。
- 页面不删、不 301、状态码不变；`public/*.html` 未动；sitemap 未动（本来就只含英语 URL）。

### 验证

- `npx tsc --noEmit` exit=0；`npm run lint` 0 error / 24 warning（与基线同数）。
- `npm run build` 成功（640 个 prerender HTML，其中 576 个为语言副本 = 90%），抽查产物：`en/0-c-to-f` = `index, follow` + 2 条 alternate；`/es` `/ja/0-c-to-f` `/de/20-c-to-f` `/es/fahrenheit-to-celsius` = `noindex, follow` + 0 条 alternate。

### 方向变更（写入台账以免后续重复讨论）

本域名不再作为收入载体；只保留“仪表盘周测 + GSC UI 手动 Request Indexing（可选）”。止损线：2026-11-19 若 `sitemap indexed` 仍为 0 则停止一切主动投入。详见 [docs/收录诊断-2026-08-27.md](./收录诊断-2026-08-27.md) 第 4-5 节。

---

## 2026-08-26：变现层落地（方案 A）+ 三项收入相关缺陷修复

**决策**：站长选 A——零流量期先把变现层与度量建好，流量回场即开始收钱；不再等「Bing 月点击 > 300」这类当前不可达的触发条件。

### 新增：变现层（默认全关，配置驱动）

- `config/monetization.json`：全站唯一变现入口，ads.enabled / affiliate.enabled 均为 false。
- `utils/monetization.ts`：读取口。`adsenseShouldLoad()` 要求「开关 + client + 至少一个非空 slot」三者齐备，缺一不渲染。
- `components/Monetization/`：`<ins class="adsbygoogle">`（data-ad-format=fluid + 预留 90px 防 CLS）与联盟卡片（rel="sponsored noopener nofollow" + 披露语）。关闭时返回 null——无 DOM、无第三方请求。
- 挂载点（固定，符合「H1 与答案区之间禁放」）：
  - `components/TemperaturePage.tsx`：customSections/ConversionTable 之后、EnhancedFAQ 之前 → 覆盖全部温度页（含 RoomTemperaturePage 系列）
  - `components/FahrenheitToCelsiusPage.tsx`：customSections 之后 → 覆盖 93-f-to-c / 375-f-to-c
  - `pages/fan-oven-conversion-chart.tsx`（variant=chart, cluster=oven）、`pages/body-temperature-chart-fever-guide.tsx`（variant=guide, cluster=body）
- `components/Analytics.tsx`：adsbygoogle.js 改为仅在配置开启时注入——「Auto Ads 永久关闭」由后台开关升级为代码护栏。
- `public/*.html`：86 个旧静态页删除 AdSense 装载代码（GA 保留）。逐文件对内联脚本做语法校验，通过才写回。

### 新增：GA4 业务事件（`utils/track.ts`）

`conversion_completed` / `copy_result`（Converter + 温度页 + F→C）/ `chart_download`（6 处 PDF + ReferenceSection）/ `related_page_click`（相邻温度块 + 正文内链）/ `reverse_conversion_used`（F→C 输入）。SSR 端 no-op；`trackOnce` 防同会话重复上报。
**待站长**：GA4 后台把这 5 个事件登记为 Key event，否则不进转化报表。

### 修复

1. `utils/temperatureContext.ts` `formatLabel`：SCREAMING 枚举被当作 camelCase 拆分，输出 `w e a t h e r`，且已渗进线上 /0-c-to-f 的 meta / og / twitter / JSON-LD。首版只修了含 `_` 的分支，else 分支残留（详见下方补记），现已整体重写。
2. `components/TemperaturePage.tsx`：description 不再拼接机器生成的 context 句；新增 `clampToSentence`（155 字符按句边界收口），修掉 /4-c-to-f、/75-c-to-f 那种在半个单词处被 SERP 截断的超长摘要。
3. `utils/textSpinner.ts`：`getVariantIndex` 恒返回 0，按 celsius 偏移挑同义变体的伪原创机制停用（AGENTS.md 同义替换红线）；`generateOGDescription` 不再回退 spinner；`TemperaturePage` 的 `disableSmartFaqs` 默认改 true（此前 /37-5-c-to-f 漏传，线上在出轮换 FAQ）。
4. `styles/globals.css`：`.faq-answer` 由 `display:none` 改为 `max-height:0;overflow:hidden`，与 oven 系列组件统一，AI 爬虫可读折叠答案。原计划「收录打通后」再做，因双引擎 0 展示、GEO 已成主通道而提前。

**验证**：`npx tsc --noEmit` 通过；`npm run lint` 0 error / 24 warning（与改动前同数）；本地 `next dev` 实测 /0-c-to-f、/180-c-to-f、/fan-oven-conversion-chart、/body-temperature-chart-fever-guide、/175-c-to-f.html 全 200；把开关临时置 true 时三处挂载分别输出 slot 9876543210 / 1111111111 / 2222222222 与带 tag 的联盟链接，置 false 时页面 0 命中；`public/` 内 pagead2|adsbygoogle 残留 0。

**下一步（变现侧）**：AdSense 后台建 3 个手动广告单元并回填 slot → 核对并填写 oven/body 两条线的真实商品链接与 Associates tag → GA4 建 Key event → 部署。`celsius-to-fahrenheit-chart` / `fever-temperature-chart` 是否加位待有数据后再定。

### 复核补记（同日 19:08，站长指出修复不完整）

- `formatLabel` else 分支残留 bug：`key.replace(/([A-Z])/g, ' $1')` 对连续大写无效，`WEATHER → W E A T H E R`。影响面比首版判断更大——六个 scene name 里 `BODY` / `WEATHER` / `WATER` / `OVEN` 均无 `_`，全部走错分支。
- 最终实现（`utils/temperatureContext.ts`）：先去 `[_ ]+`、再按 camelCase 边界 `([a-z])([A-Z])` 插空格（必须在大写归一之前），最后 `toLowerCase()` + `\b\w` 逐词首字母大写。实测全量输入：`WEATHER→Weather`、`BODY→Body`、`EXTREME_COLD→Extreme Cold`、`normalBodyTemp→Normal Body Temp`（与旧 camelCase 路径结果一致，无回归）、`undefined→Standard`；36 个输入碎字模式 0 命中。
- `utils/textSpinner.ts`：`getVariantIndex` 之后残留一段不可达的重复 `return`（同块写了两遍），已删。
- **关键发现**：`components/TemperaturePage.tsx:1048` 的 `{/* {renderGranularInsight()} */}` 自 2026-02-12（commit `0053ba2`）起被注释——「💡 Analysis: {label}」卡片根本不渲染，所以 `formatLabel` 的 label 输出目前无用户可见出口；碎字泄漏的唯一活路是 `getDescriptionForScene` → `granularContext.description` → 拼进 meta description（同日修复 2 已断）。因此本地页面搜不到碎字并不等于线上干净：`curl https://ctofconverter.com/0-c-to-f` 实测 meta 与 JSON-LD 仍为 `... 0°C is a significant w e a t h e r benchmark.`，需部署后才消失。
- 顺带修掉 og 兜底缺口：`TemperaturePage` 的 `ogDescription` 旧逻辑只看 `strategy.meta.ogDescription`，缺失即退回站级模板句，导致 /37-5-c-to-f、/36-c-to-f 等页 description 具体、og/twitter:description 却是全站通用句。现改为 `strategy.meta.ogDescription → customMetaDescription → customDescription → generateOGDescription`；实测 /37-5 与 /36 的 og 已变成本页描述且与 twitter 一致。
- 旧 `public/*.html` 静态页实测无碎字（`w e a t h e r|b o d y|o v e n` 0 命中），无需回填修补。
- 复核口径：不依赖记忆——从源码里抽出 `formatLabel` 实际文本 eval 后跑全量输入；渲染侧用 `next dev -p 3111`（避开站长其他项目的 3000）实测 6 页 `spacedRuns=0`。`tsc` 通过，lint 仍 0 error / 24 warning。

---

## 2026-08-05：谷歌数据分析 + AdSense 配置调整

### 改动 1：拉取 GSC + GA4 数据

运行 `node scripts/fetch-analytics.mjs`（OAuth token 已就位），生成 `docs/谷歌数据/2026-08-05-28d.json`。

**核心数据**（2026-07-08 ~ 08-05）：
- GSC 点击 0，展现 56，平均排名 88.26
- GA4 用户 117，Google 自然流量 0
- 移动端跳出率 100%，平均停留 1 秒
- 实际流量全来自旧 `.html` 页面（Bing/Yahoo/Ecosia）

### 改动 2：AdSense 后台关闭 Auto Ads + Auto Optimization（用户操作，非代码改动）

**问题诊断**：通过浏览器实测 `45-c-to-f.html`，发现 9 个 AdSense Auto Ads 自动投放，广告 #1 在 H1 和转换结果之间，移动端首屏被广告占满 → 100% 跳出 → Google Page Experience 降权 → 排名 80+。

**操作**：AdSense 后台 → Ads → 关闭 Auto Ads；Optimization → 关闭自动优化。

**预期**：1 小时生效；28-60 天后 CrUX 数据更新，Page Experience 信号恢复。

### 发现：14 个高流量页面无 Next.js 版本（301 方案被堵住）

GA4 Top 14 高流量页面（45/60/120/175/210/220/230/105/44/48/76/90/150/190-c-to-f）**全部只有旧 HTML，无 .tsx**。无法 301 到不存在的页面。需优先补建 Next.js 版本。

> 详细分析与策略调整见 `docs/修复方案（执行中）.md` 第「零」节。

---

## 2026-07-16：技术环境准备

### 改动 1：新建 `config/quality-pages.json`

记录已精做的手写高质量页面，sitemap 脚本读取此文件分配 priority。

```json
{
  "qualityPages": [
    "oven-temperature-conversion",
    "oven-to-air-fryer"
  ],
  "lastUpdated": "2026-07-16"
}
```

### 改动 2：重写 `scripts/generate-sitemap.js`

**问题**：`getSitemapPages()` 返回硬编码 3 个页面（`SITEMAP_PAGES = ['index', 'oven-temperature-conversion', 'oven-to-air-fryer']`），`getAllPages()` 函数定义了但从未被调用。sitemap.xml 只有 3 条 URL，60+ 个页面无法被 Google 发现。

**改动内容**：

1. 新增 `getTemperaturePages()` 函数，扫描 `pages/` 目录下以数字开头的 `.tsx` 文件
2. 重写 `getSitemapPages()`，合并 `getAllPages()` + `getTemperaturePages()`，精做页面排最前
3. 重写 `getPriority()`：首页 1.0 > 精做页面 0.9 > 工具页 0.8 > 整数温度页 0.6 > 小数温度页 0.4 > 其他 0.5
4. 移除多语言支持：删除 `LOCALES`、`NON_EN_EXCLUDED`、`SITEMAP_PAGES` 常量，删除 `toHreflang()`、`getAvailableLocales()`、`getPageRollupLastMod()` 函数
5. 生成循环只输出英语 URL，移除 hreflang 标签和 `xmlns:xhtml` 命名空间
6. 从 `config/quality-pages.json` 读取精做页面列表
7. 排序：首页最前 -> 精做页面 -> 其余按自然顺序

**结果**：sitemap.xml 从 3 条 URL 变为 61 条英语 URL

### 改动 3：更新 `public/robots.txt`

**改动**：`Allow: /_next/static/` -> `Disallow: /_next/static/chunks/` + `Allow: /`

**理由**：GSC 抓取记录显示两周内 Google 抓取了 40+ 个不同 JS chunk 文件，纯浪费抓取预算。保留 `/_next/static/css/` 和 `/_next/static/media/` 的抓取权限。

**改动前**：
```
User-agent: *
Allow: /_next/static/
Disallow: /_next/data/
Disallow: /_next/server/
Disallow: /_next/cache/
Disallow: /api/
Disallow: /_vercel/
```

**改动后**：
```
User-agent: *
Disallow: /_next/static/chunks/
Disallow: /_next/data/
Disallow: /_next/server/
Disallow: /_next/cache/
Disallow: /api/
Disallow: /_vercel/
Allow: /

Sitemap: https://ctofconverter.com/sitemap.xml
```

### 改动 4：重新生成 `public/sitemap.xml`

运行 `node scripts/generate-sitemap.js`，生成 61 条英语 URL 的 sitemap。

**验证结果**：
```
URLs:          61
Quality pages: 2 (priority 0.9)
File size:     10.2 KB
```

**sitemap 排序**：
1. `/` 首页 - priority 1.0
2. `/oven-temperature-conversion` - priority 0.9
3. `/oven-to-air-fryer` - priority 0.9
4. `/fan-oven-conversion-chart` - priority 0.8
5. `/c-to-f-calculator` - priority 0.8
6. 温度页（0.6 / 0.4）
7. 其他内容页（0.5）

---

## 待办（下一步）

- 部署到生产环境
- 在 GSC 提交新的 sitemap.xml
- 开始逐页制作高质量内容（第一个：`180-c-to-f`）
