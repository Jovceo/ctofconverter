# 2026-08-05

## 新建 /375-f-to-c 页面（烘焙意图 F→C）
- 用户按大纲建了 `pages/375-f-to-c.tsx` + `locales/en/375-f-to-c.json`，复用 `FahrenheitToCelsiusPage` 组件 + JSON 数据驱动。
- 375°F = 190.6°C ≈ 190°C = Gas Mark 5。搜索意图：烘焙/烤箱（食谱写 375°F 想知道摄氏+烤什么），与 350/400/425 构成烘焙 F→C 簇，各页食物不同避免"换数字"模式。
- 结构：Answer Capsule → Why 375（甜点区理论）→ 烘焙时间表(8食物, King Arthur/USDA) → 烤箱类型转换(Fan 355°F/171°C, Air Fryer 同, GM5) → 温度对比(350/375/400/425) → 公式 → FAQ 4条。
- 内链走烘焙簇（oven-temperature-conversion / fan-oven / oven-to-air-fryer / 180-c-to-f / c-to-f-calculator），不走医疗簇。

## 组件修复（前置阻塞已解）
- `FahrenheitToCelsiusPage.tsx` 第 297 行原本硬编码医疗免责声明"call 911"，`howToName` 默认医疗名。新增 `customDisclaimer?: string` prop，未传回退医疗声明（93-f-to-c 不受影响）。375 页传烹饪版免责（含 USDA）。
- 背景：该组件最初为 93-f-to-c（医疗+天气）写，直接用于烘焙页会出"烤饼干 call 911"的荒谬声明。

## 注册 & 验证
- `config/quality-pages.json` + `pages/index.tsx` QUALITY_PAGE_INFO 均加入 375-f-to-c（首页显示链接，否则零入链）。
- sitemap 重新生成：63 URL，375-f-to-c priority 0.9 + changefreq weekly。
- `npx tsc --noEmit` 通过（独立验证 exit 0）。

## 待修小问题（已修复，2026-08-05 复查确认）
1. ✅ metaDescription 117 字符 ≤ 120 上限。
2. ✅ tempCompare 375 行 celsius 已改为 190.6°C，与正文统一。
3. ✅ DISCLAIMER 已去掉硬编码 "July 2026"，保留烹饪版（含 USDA 来源），日期改由组件 git 自动显示。
- 复查方式：磁盘读取 + python 计数字符数 + tsc exit 0，全部通过。375 页可发布。
- 发布后建议去 GSC 对本页"请求编入索引"（零入链，sitemap 已含）。

## 遗留
- `350-f-to-c` 仍半残：只有孤立组件 `FahrenheitOvenConverter.tsx`，页面文件从未建。建议用 FahrenheitToCelsiusPage+JSON 路线补齐并删孤儿组件。
- 烤箱三页（oven-temperature-conversion / oven-to-air-fryer / fan-oven-conversion-chart）仍"已抓取未收录"，根因=sitemap 稀疏+三页蚕食+零入链。375 页内链已指向它们（出站），但需它们反向链回 375 才能给权重（动旧文件违反 AGENTS.md，待定）。
