# 2026-07-10

## SEO/content fixes — /oven-to-air-fryer & /oven-temperature-conversion
Reviewed both pages from UX + SEO angles. User explicitly said "不需要做多语言" → skipped the hreflang fix.

Applied edits:
- `pages/oven-temperature-conversion.tsx`:
  - Fixed Answer Capsule range claim: "50°C to 300°C" → "120°C to 260°C" (now matches actual chart).
  - Unified title & ogTitle to include "Air Fryer" (was "Fan Oven" only, mismatched H1 which said "Fan & Air Fryer").
  - Removed redundant 2nd table "Common Cooking Temperatures Reference" (its rows duplicated the main chart's Common Use column).
- `pages/oven-to-air-fryer.tsx`:
  - Title suffix changed from "— °C to °F Converter" (keyword dilution) → "— °C, °F & Time Chart".
  - Air fryer table column now shows both °C and °F (added inline `Math.round(airFryer*9/5+32)`).
  - Added note explaining recipe °F rounding (e.g. 180°C≈350°F) to reconcile capsule (rounded) vs chart (exact).

Known remaining (left as-is): `seo.alternates` still overrides Layout's auto 10-locale hreflang set with only en+x-default. Per user, no multilingual work.

## Re-added dish table (info gain)
User pointed out the deleted "Common Cooking Temperatures Reference" table was a real info gain (dish→temp→Gas Mark mapping rivals rarely combine). Re-added as "Oven Temperature by Dish" section: Dish + Conventional °C/°F + Gas Mark; Fan column dropped (redundant with main chart). Placed after "Temperature Guide by Oven Type", before FAQ. Serves dish-indexed search intent vs the temp-indexed main chart. User delegated the column choice ("你来选") → went with recommended keep-Gas-Mark version.

## Three review-raised defects FIXED (agent applied; user's own edits never landed on disk)
1. **Converter unstyled on air-fryer page (P0)**: `OvenTemperatureConverter` uses literal global classNames (`.oven-converter*`, `.oven-result-*`), but the `:global(...)` rules lived only in `styles/oven-temperature-conversion.module.css` (CSS modules only load per-page). Moved all 11 global rules into `styles/globals.css` (already imported globally via `_app.tsx`) and removed them from the page module. Now both pages get converter styles — single source of truth, no duplication.
2. **Gas Mark data accuracy**: 170°C `gas:'3'`→`'3–4'`, 210°C `gas:'6'`→`'6–7'` (UK standard: GM3=160/GM4=180, GM6=200/GM7=220; 170 & 210 fall between marks). Also fixed the "Oven Temperature by Dish" table (Sponge cake 170°C → 3–4) to stay consistent.
3. **35°F approximation bug**: "20°C (or 35°F)" is imprecise (20×9/5=36). Replaced all 6 occurrences in `oven-to-air-fryer.tsx` with 36°F. NOTE: `oven-temperature-conversion.tsx` never had 35°F.

Lesson: user reported "already fixed" twice but changes were NOT on disk (likely unsaved / wrong path). Trust disk+git verification, not the user's status table.
